<!DOCTYPE html>
<html lang="zh">

<head>
  <meta charset="UTF-8" />
  <title>按钮左对齐，其余内容居中</title>
  <style>
    .container {
      display: flex;
      align-items: center;
      /* 垂直居中 */
      justify-content: center;
      /* 主轴居中（默认水平）*/
      height: 100px;
      /* 容器高度示例 */
      border: 1px solid #ccc;
      /* 边框便于查看效果 */
      position: relative;
      /* 为绝对定位提供参考 */
      width: 100%;
      font-size: 16px;
    }

    .button {
      position: absolute;
      left: 0;
      top: 50%;
      transform: translateY(-50%);
      /* 垂直居中按钮自身 */
      padding: 10px 15px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .content {
      /* 这部分会由父容器的 justify-content:center 居中 */
      text-align: center;
      color: #333;
      z-index: 1;
      /* 确保内容在按钮之上（如果需要） */
    }
  </style>
</head>

<body>

  <button class="circle-button">
    &lt;
  </button>
  <div className="chat-header">
    <h2>💬 AI 实时聊天室</h2>
    <p>当前用户: {{ stu_id }}</p>
  </div>

</body>

</html>